# 0210 merged-main device evidence

Roadmap task 0210 is evaluated against merged `main` at
`5c99b059249784f450caf14255b79cf1eb4bb300` (PR [#209](https://github.com/AlisinaDevelo/LOOM/pull/209),
merged 2026-08-24 20:04:31 UTC). The run was performed on the target Mac:

- macOS 26.6.2 (build 25G83), arm64;
- Rust/cargo 1.96.0, Node.js v26.7.0, npm 11.19.0; and
- evidence directory `/tmp/loom-0210-merged.RJayqQ`.

The corpus is synthetic and CC0-1.0. It contains no user documents, copied publications,
external fonts, or screen captures. The MVP demo reuses the checked-in `ocr-cropped.png`
fixture; no full-screen screenshot was captured or retained.

## Acceptance mapping

| Criterion | Retained evidence | Result |
| --- | --- | --- |
| Rights-clean coverage for tagged text, multi-column pages, ligatures, rotated pages, encrypted files, malformed files, and image-only pages | `benchmarks/pdf-adversarial/corpus/manifest.json`; fixture IDs `tagged-text`, `multi-column`, `ligature`, `rotated-page`, `encrypted`, `malformed`, `image-only`; `generate-a`/`generate-b` comparison | Seven fixtures generated byte-for-byte identically; indexed classes are tagged text, multi-column, ligature, and rotated page; encrypted, malformed, and image-only inputs fail closed as unsupported. |
| Expected page identity, warnings, byte hash, extractor version, and unsupported outcome | `pdf-adversarial.json`; manifest SHA-256 values; `pdf-rust-test.log` | Runner reports 7/7 complete, 4 indexed, 3 unsupported, 0 failures; indexed fixtures match `loom.pdf`/`0.1.0`, page 1 anchors, and empty warning lists. Existing parser regression is 2 passed / 0 failed. |
| Changed fixtures are rejected and per-class completeness/failure counts are emitted | `drift.json`, `drift_exit=2`, and `pdf-adversarial.json` | Flipping one byte in `ligature.pdf` produces a hash-mismatch failure and exit 2; the report retains per-class expected/indexed/unsupported/failure counts. |

## Reproduction

The retained command list is `commands.txt`. The relevant commands were:

```text
python3 scripts/generate-pdf-adversarial-fixtures.py --output <temp-a>
python3 scripts/generate-pdf-adversarial-fixtures.py --output <temp-b>
diff -ru <temp-a> <temp-b>
python3 scripts/pdf-adversarial.py --loom target/debug/loom
python3 scripts/pdf-adversarial.py --loom target/debug/loom --manifest <drifted-manifest>
CARGO_PROFILE_TEST_DEBUG=0 CARGO_PROFILE_TEST_STRIP=symbols CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=1 cargo test -p loom-core --test pdf_extraction --locked --offline -- --nocapture
CARGO_PROFILE_DEV_DEBUG=0 CARGO_PROFILE_DEV_STRIP=symbols CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=1 ./scripts/demo-mvp.sh
python3 scripts/roadmap.py --validate-only
```

The MVP demo recovered a text passage, a PDF page anchor, and an OCR image region from five
selected artifacts with zero indexing failures; it reported 5 artifacts and 7 passages. Its
viewer instructions intentionally reference a cropped OCR fixture, not a desktop capture.

Roadmap validation also passed: 154 active issues, 4 retired issues, 20 milestones, 13 phases,
141 parent edges, and 314 dependency edges.

## Evidence digests

The retained SHA-256 digests are:

```text
pdf-adversarial.json  d96056dc309a6114c4b99d3bc3f9e51ce452f664445d36a1b47dc82eeea874cf
drift.json            1d34e3c846d363df36412060b936cda3079d17c93ef0701a50fd39bb003cae6f
pdf-rust-test.log     7b8da718cdd345eb36d934036d66e008ee8630f1b138815d285e01b04c5ba404
mvp-demo.log          a4fdd39261d4e8eb64f2dc36ef32277c72a3bb9a0990fe93c4d209476d023e5f
roadmap-validate.json df36f3da734b1902d0f0e6711aeb03645bc9f04afad31993b9275c55f1a82c96
```

This evidence validates the PDF ingestion contract and local MVP path. It does not claim
password decryption, arbitrary production-PDF coverage, an interactive Screen Recording
permission session, or a packaged Tauri artifact; those remain separate gates.
